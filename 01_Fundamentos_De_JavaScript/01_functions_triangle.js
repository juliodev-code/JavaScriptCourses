let base = 5;
let altura = 7;

const triangleArea =  (base, altura) =>{ base *  altura / 2 };

console.log(`El area de un trianglo de base ${base} y altura ${altura} = ${ triangleArea(base, altura) } `);


